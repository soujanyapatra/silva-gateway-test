import React, { ReactNode } from 'react';
import { Area } from './sources';
declare type State = {
    areas: Area[];
};
export declare const configContext: React.Context<State>;
export declare type AreaFilter = (value: Area, index: number, array: Area[]) => boolean;
export declare type AreaMapper = (value: Area, index: number, array: Area[]) => Area;
export declare type AreaSorter = (a: Area, b: Area) => number;
export declare const ConfigProvider: ({ children, locale, areaFilter, areaMapper, areaSorter, }: {
    children: ReactNode;
    locale?: any;
    areaFilter?: AreaFilter | undefined;
    areaMapper?: AreaMapper | undefined;
    areaSorter?: AreaSorter | undefined;
}) => JSX.Element;
export {};
