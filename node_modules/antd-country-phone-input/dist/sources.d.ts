import { ReactNode } from 'react';
export declare const getAreas: (locale: any) => {
    name: string | undefined;
    short: string;
    phoneCode: number;
    emoji: ReactNode;
}[];
export declare type Area = {
    short: string;
    phoneCode: number;
    emoji: ReactNode;
    name?: string;
};
export declare const defaultAreas: Area[];
export default defaultAreas;
